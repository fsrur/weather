import React from "react"

function Instructions() {
  
  return(
    <div>
      <p className="instructions">This is a React app, where a zip is used to fetch the weather data and set the state for each piece of information displayed. In total there are four different components: the header, the button, the main weather forecast and this comment section.</p>
    </div>
  )
}


export default Instructions